#ifndef _MALLOC_CONF_H_
#define _MALLOC_CONF_H_

#define MALLOC_BLOCK_SIZE 32                                          // 缓冲块大小
#define MALLOC_BLOCK_COUNT 40                                       // 缓冲池大小
#define MALLOC_POOL_SIZE (MALLOC_BLOCK_SIZE / 4) * MALLOC_BLOCK_COUNT // 缓冲池大小
#define MALLOC_BLOCK_COUNT_SIZE uint16_t                               //块数量的宽度

#endif
